# 가상환경 — uv sync가 만든다
.venv/

# 파이썬
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# 머신마다 다른 값 (포트, 카메라 인덱스). 각자 만든다.
local.ps1

# 학습 산출물 — 체크포인트 하나가 600MB. 허깅페이스 Hub를 쓸 것
outputs/
wandb/

# 데이터셋 — Hub에 있다
data/
*.parquet
*.mp4

# 비밀
.env
*.pem

# OS / 에디터
.DS_Store
Thumbs.db
.idea/

# 주의: third_party/lerobot 은 일부러 커밋한다. 여기에 넣지 말 것.
