# 작업 방법

어디서 작업하고, 어떻게 `master` 에 넣는지.

처음이면 [docs/1_setup.md](docs/1_setup.md) 로 환경부터 준비할 것.

---

## 메인테이너

**메인테이너**는 이 프로젝트를 관리하는 사람이다. 현재는 `@jsjin7371`.

하는 일:

- `master` 브랜치를 안전하게 유지한다
- 올라온 PR 을 검토하고 승인한다
- `third_party/` 의 lerobot 버전을 올린다
- `docs/`, `pyproject.toml` 등 공용 파일을 관리한다

**부원은 메인테이너가 아니다.** 아래 "작업 구역" 밖을 고치고 싶으면 직접 고치지 말고 이슈나 PR 로 제안한다.

---

## 작업 구역

```
projects/          ★ 여기는 자유. 마음대로 만들고 고친다
그 외 전부         메인테이너만
```

| 폴더 | 왜 보호하나 |
|---|---|
| `third_party/` | lerobot 원본 복사본. 고쳐도 다음 업데이트 때 지워진다 |
| `calibration/` | 로봇팔 기준값. 바꾸면 학습된 인공지능이 엉뚱하게 움직인다 |
| `pyproject.toml` · `uv.lock` | 전원이 같은 환경을 쓰게 하는 장치 |
| `docs/` · `tools/` | 공용 문서와 도구 |

보호 구역을 건드린 PR 은 **메인테이너 승인 없이는 머지되지 않는다.** GitHub 가 자동으로 막는다.

실수로 고쳤어도 문제없다. 되돌리면 된다.

```powershell
git checkout -- third_party/
```

---

## AI 도구를 쓸 때

Claude Code, Cursor, Copilot 등을 쓴다면 **작업 전에 [AGENTS.md](AGENTS.md) 를 읽게 한다.**

```
AGENTS.md 를 먼저 읽고 시작해줘
```

보호 구역, `uv` 사용 규칙, 하드웨어 안전 규칙이 거기 들어 있다. 안 읽히면 AI 가 `third_party/` 를 고치거나 없는 옵션을 지어낼 수 있다.

**AI 가 쓴 코드도 내 책임이다.** 돌아가는 것만 확인하고 넘기지 말고 왜 그렇게 되는지 읽어 볼 것.

특히 `feat/` 브랜치는 ③단계에서 **세미나에서 직접 설명해야 한다.** 이해하지 못한 코드는 결국 본인이 막힌다.

---

## 브랜치

### 규칙

```
master                                  ← 여기에 직접 푸시하지 않는다
└─ <종류>/<깃허브-아이디>/<주제>          ← 여기서 작업한다
```

`<종류>` 는 **아래 셋 중 하나**다. 무엇을 고쳤느냐가 아니라 **PR 을 어디까지 거칠 것이냐**로 고른다.

| 종류 | 언제 | PR 단계 |
|---|---|---|
| `feat/` | 새 프로젝트, 새 기능 | ① ② ③ 전부 |
| `docs/` | 문서 수정 | ① 만 |
| `fix/` | 잘못된 것 바로잡기 | ① 만 |

커밋 메시지에는 `refactor:`, `chore:`, `test:` 도 쓰지만 **브랜치 접두사는 이 셋뿐이다.** 코드 정리나 설정 변경은 `fix/`, 새로 만드는 것은 `feat/` 로 두면 된다.

이 문서에서 `<...>` 로 감싼 부분은 **자기 값으로 바꿔 쓰는 자리**다. 꺾쇠까지 지우고 쓴다.

| 자리 | 무엇을 넣나 | 예 |
|---|---|---|
| `<깃허브-아이디>` | 내 GitHub 계정 이름. 팀이면 팀 이름 | `jsjin7371`, `vision-team` |
| `<주제>` | 영어 소문자. 띄어쓰기는 하이픈 | `hand-tracking` |
| `<번호>` | `projects/` 의 다음 순번. 두 자리로 | `03` |

채우면 이런 모양이 된다.

```
feat/jsjin7371/hand-tracking
feat/vision-team/object-detect      팀으로 작업하는 경우
docs/kim/basic-workflow-typo        문서 오타 수정
```

**브랜치 하나 = PR 하나.** 작업이 끝나면 브랜치도 끝난다. 계속 쓰는 브랜치를 만들지 않는다.

폴더 이름과 맞춰 두면 알아보기 쉽다.

```
feat/jsjin7371/hand-tracking   →   projects/03-hand-tracking/
```

### 시작하기

```powershell
git checkout master
git pull                                        # 최신으로 맞추고
git checkout -b <종류>/<깃허브-아이디>/<주제>     # 새 브랜치 만들기
```

아래에서 `<내-브랜치>` 는 방금 만든 이 이름을 가리킨다.

작업하면서 중간중간 저장한다.

```powershell
git add .
git commit -m "<무엇을 했는지 한 줄>"
git push -u origin <내-브랜치>                   # 첫 푸시는 -u 를 붙인다
```

다음부터는 `git push` 만 하면 된다.

<details>
<summary>실제로 채우면 이렇게 된다</summary>

```powershell
git checkout master
git pull
git checkout -b feat/jsjin7371/hand-tracking

git add .
git commit -m "feat: 손 관절 좌표 추출 추가"
git push -u origin feat/jsjin7371/hand-tracking
```
</details>

### 커밋 메시지 규칙

앞에 **무엇에 관한 변경인지**를 한 단어로 붙인다. 업계에서 널리 쓰는 방식이다.

```
<종류>: <무엇을 했는지>
```

| 종류 | 언제 |
|---|---|
| `feat:` | 새 기능 |
| `fix:` | 버그 수정 |
| `docs:` | 문서 |
| `refactor:` | 동작은 그대로, 코드 정리 |
| `chore:` | 설정·패키지 등 잡일 |
| `test:` | 테스트 코드 |

```
feat: 손 관절 좌표 추출 추가
fix: 카메라 인덱스가 바뀌면 죽는 문제 수정
docs: 실행 방법에 준비물 목록 추가
```

종류는 영어로, 내용은 한국어로 쓴다. `수정`, `ㅇㅇ`, `update` 같은 메시지는 나중에 찾을 때 아무 도움이 안 된다.

자세한 건 [docs/4_git.md](docs/4_git.md) 참고.

### 팀으로 작업할 때

한 명이 브랜치를 만들고 푸시하면, 나머지는 그 브랜치를 받아서 쓴다.

```powershell
git fetch
git checkout <브랜치-이름>             # 만든 사람에게 이름을 물어본다
```

같은 브랜치에 여러 명이 푸시하므로, **푸시 전에 `git pull` 을 먼저 한다.**

### 작업이 오래 걸리면

`master` 가 바뀌었을 수 있다. 가끔 최신 내용을 가져온다.

```powershell
git checkout master
git pull
git checkout <내-브랜치>                      # 내 브랜치로 돌아와서
git merge master                             # master 내용을 합친다
```

미리미리 하면 충돌이 작다. 몰아서 하면 커진다.

---

## 커밋하면 안 되는 것

아래는 `.gitignore` 에 등록돼 있어서 자동으로 걸러진다. 왜 그런지만 알아 두면 된다.

| 무엇 | 왜 |
|---|---|
| `outputs/` | 학습 결과. 파일 하나가 600MB 다 |
| `*.mp4`, `*.parquet` | 녹화 데이터. Hugging Face 에 올린다 |
| `.venv/` | `uv sync` 로 언제든 다시 만들어진다 |
| `local.ps1` | 컴퓨터마다 다른 값(포트 번호 등) |
| `*.pem`, `.env` | 비밀번호·열쇠 종류 |

푸시하기 전에 뭐가 올라가는지 한 번 본다.

```powershell
git status
```

---

## PR — 검토를 통과해야 머지된다

작업이 끝나면 GitHub 에서 `master` 로 Pull Request 를 연다.

> **Pull Request(PR)** 는 "내 브랜치 내용을 `master` 에 넣어 달라"는 요청이다.
> 바로 합쳐지지 않고, 검토를 거친다.

### 3단계를 다 거치는 경우

**새로 가르칠 것이 생겼을 때만** 3단계를 전부 돈다. 즉 `feat/` 브랜치다.

문서 수정(`docs/`)과 버그 수정(`fix/`)은 **① 만 통과하면 머지된다.** 오타 하나 고치는 데 세미나 발표까지 하면 아무도 문서를 안 고치게 된다.

특히 ②단계에서 "여기 안 돼요" 하고 막힌 사람이 **직접 고쳐서 PR 을 올리는 것**이 가장 좋은 흐름이다. 그게 어려우면 문서는 계속 틀린 채로 남는다.

### 이 프로젝트가 PR 에서 보는 것

코드가 돌아가는지만 보는 게 아니다.

> **"이 폴더를 처음 보는 사람이, 이것만 읽고 따라 할 수 있는가?"**

동아리 자산으로 남기는 게 목적이라, **가르칠 수 있는 상태인지**를 본다. 3단계가 그걸 확인하는 절차다.

### ① 코드와 문서 검토

| 누가 | AI 도구 + 메인테이너 |
|---|---|
| 본다 | 코드에 오류가 없는지, 문서가 충분한지 |

문서가 부족하면 여기서 돌아온다. 아래 "필수 항목"을 채웠는지 스스로 먼저 확인할 것.

아직 작업 중이면 **Draft PR** 로 열어 둔다. 준비되면 `Ready for review` 를 누른다.

### ② 다른 컴퓨터에서 재현

| 누가 | **누구든 가능** — 내 컴퓨터가 아니기만 하면 된다 |
|---|---|
| 본다 | 문서만 보고 따라 해서 실제로 동작하는가 |

확인해 주는 사람은 이렇게 한다.

```powershell
git fetch
git checkout <확인할-브랜치>
uv sync
# 그 프로젝트 README 의 "실행 방법" 을 그대로 따라 한다
```

그리고 PR 에 댓글로 결과를 남긴다.

```
확인함 (RTX 3060 / 윈도우 11)
- uv sync 정상
- README 3번 명령에서 카메라 번호를 바꿔야 했음
- 나머지는 그대로 동작
```

**막힌 지점이 있으면 그게 문서에 빠진 부분이다.** 문제를 찾아 주는 게 이 단계의 목적이니, 안 된다고 말하는 걸 주저하지 말 것.

확인해 주는 사람도 그 프로젝트를 배우게 되므로, 서로 해주면 좋다.

### ③ 세미나 발표

**`feat/` 브랜치만 해당된다.**

| 누가 | 작성자 |
|---|---|
| 본다 | 사람 앞에서 설명할 수 있는가 |

무엇을 만들었고, 왜 그렇게 했고, 뭐가 어려웠는지 이야기한다. 발표가 끝나면 메인테이너가 머지한다.

### 진행 상황은 라벨로

| 라벨 | 뜻 |
|---|---|
| `1-검토중` | 코드·문서 검토 중 |
| `2-재현확인` | 다른 컴퓨터 확인 대기 |
| `3-발표대기` | 세미나 발표 대기 |

### 머지된 뒤

브랜치를 지운다. GitHub 에 뜨는 `Delete branch` 버튼을 누르면 된다.

```powershell
git checkout master
git pull
git branch -d <내-브랜치>                     # 내 컴퓨터에서도 삭제
```

---

## 프로젝트 폴더에 들어가야 할 것

`projects/_TEMPLATE/` 을 복사해서 시작하면 항목을 빠뜨리지 않는다.

```powershell
Copy-Item -Recurse projects\_TEMPLATE projects\<번호>-<주제>
```

예: `Copy-Item -Recurse projects\_TEMPLATE projects\03-hand-tracking`

### `README.md` 필수 항목 6개

| | 항목 | 왜 |
|---|---|---|
| 1 | 무엇을 하는가 (+ 사진이나 영상) | 처음 보는 사람이 30초 안에 파악 |
| 2 | 왜 만들었는가 | 맥락 없는 코드는 가르칠 수 없다 |
| 3 | 실행 방법 | ②단계 재현의 근거. 복사해서 붙여 넣을 수 있게 |
| 4 | 어떻게 동작하는가 | 파일 구조와 흐름 |
| 5 | **막혔던 것과 해결 방법** | ★ 아래 참고 |
| 6 | 한계와 다음에 할 것 | 다음 사람이 이어받을 지점 |

### 5번이 가장 중요하다

**성공만 적힌 문서는 가르치지 못한다.**

```
잘 안 돼서 하루를 날렸다. 원인은 윈도우 개발자 모드가 꺼져 있던 것.
켜니까 바로 됐다.  (에러: OSError: [WinError 1314])
```

이런 기록 한 줄이 다음 사람의 하루를 아낀다. 에러 메시지를 그대로 붙여 넣으면 검색으로도 찾을 수 있다.

잘 안 된 시도도 적는다. "이 방법은 안 됐다" 도 정보다.

---

## 보호 구역을 고쳐야 할 때

`third_party/` 나 `pyproject.toml` 을 꼭 고쳐야 하는 상황이 생기면, **먼저 이슈를 열어 상의한다.**

대부분은 다른 방법이 있다. 예를 들어 패키지를 하나 추가하고 싶다면, `pyproject.toml` 을 고치는 대신 이렇게 시험해 볼 수 있다.

```powershell
uv pip install <패키지>
```

이건 내 `.venv` 에만 들어가고 커밋되지 않는다. 잘 되는 걸 확인한 뒤에 정식으로 추가를 요청하면 된다.
