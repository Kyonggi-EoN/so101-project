# projects/

각자 프로젝트를 여기에 만든다. 이 폴더는 `CODEOWNERS` 보호가 걸려 있지 않아
팀원끼리 자유롭게 머지할 수 있다.

```
projects/
└─ <번호>-<이름>/
   ├─ README.md     무엇을 하는 프로젝트인지, 어떤 명령으로 돌리는지
   └─ ...
```

`third_party/`, `docs/`, `calibration/`, `pyproject.toml` 은 메인테이너만 수정한다.
